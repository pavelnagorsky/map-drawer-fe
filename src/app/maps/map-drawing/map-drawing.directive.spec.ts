import { MapDrawingDirective } from './map-drawing.directive';

describe('DrawingDirective', () => {
  it('should create an instance', () => {
    const directive = new MapDrawingDirective();
    expect(directive).toBeTruthy();
  });
});
